// 更新package.json版本号
const path = require('path');
const pkg = require('./package.json');

const packages = [
    'packages/react-eslint',
    'packages/react-hooks',
    'packages/react-ui',
    'packages/vue-eslint',
];

